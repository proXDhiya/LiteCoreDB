{
  "name": "litecoredb",
  "version": "0.1.0",
  "description": "LiteCoreDB — a minimal SQL database learning project with a Bun + TypeScript REPL.",
  "module": "src/app.ts",
  "type": "module",
  "private": true,
  "keywords": [
    "database",
    "bun",
    "typescript",
    "repl",
    "learning"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/proXDhiya/LiteCoreDB.git"
  },
  "bugs": {
    "url": "https://github.com/proXDhiya/LiteCoreDB/issues"
  },
  "homepage": "https://github.com/proXDhiya/LiteCoreDB#readme",
  "license": "MIT",
  "author": "proXDhiya",
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "start": "bun src/app.ts",
    "test": "bun test --env-file=.env.test --preload ./tests/setup.ts",
    "test:watch": "bun test --watch --env-file=.env.test --preload ./tests/setup.ts",
    "test:cov": "bun test --coverage --env-file=.env.test --preload ./tests/setup.ts",
    "test:ci": "bun test --coverage --env-file=.env.test --preload ./tests/setup.ts",
    "prepare": "husky"
  }
}
